# Implementation Plan

- [x] 1. Foundation — 学年レベルの値表現・既定値・検証ロジックを実装する
  - 学年レベルとして「中学生」「高校生」の2値のみを許容する値の種類を定義する
  - 学年レベルが未設定の場合に用いる既定値（中学生）を定義する
  - 任意の入力値が有効な学年レベルかどうかを判定できるようにする
  - ユニットテストで、有効な値（中学生・高校生）では真、無効な値（不正な文字列・undefined・null等）では偽と判定されることを確認する
  - _Requirements: 1.2, 1.4_
  - _Boundary: GradeLevel型/バリデータ_

- [ ] 2. Core — 学年レベルの永続化とAIコーチングへの反映
- [x] 2.1 (P) 学年レベルを端末（ブラウザ）に保存・読み込みするロジックを実装する
  - マウント時に保存済みの学年レベルを同期的に読み込み、SSR環境でもエラーなく動作するようにする
  - 保存値が存在しない、または不正な値の場合は既定の学年レベル（中学生）にフォールバックする
  - 学年レベルの変更操作を行うと、保存の成否に関わらずアプリ内の状態が即座に選択値へ更新される
  - 保存処理が例外をスローしても握りつぶし、アプリの動作を継続する
  - ユニットテストで、未設定時に既定値が返ること・保存済みの正常値を読み込めること・不正値保存時に既定値へフォールバックすること・保存処理が例外をスローしても状態更新自体は成功することを確認する
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 4.1_
  - _Boundary: useGradeLevel_

- [x] 2.2 (P) AIコーチの指示文を学年レベルに応じて出し分けられるようにする
  - 学年レベルを受け取り、対応する指示文を組み立てられるようにする
  - 中学生向けには中学校で学習する語彙・既習範囲を前提とした指示を含める
  - 高校生向けには高校で学習する語彙・既習範囲を前提とした、中学生向けより高度な言葉づかいの指示を含める
  - 既存のヒント中心コーチング方針（答えを直接教えない、写真分析指示等）は学年レベルに関わらず共通して含まれるようにする
  - ユニットテストで、学年レベルごとに異なる語彙レベル指示が含まれること・共通のコーチング方針指示がいずれの学年でも含まれることを確認する
  - _Requirements: 3.1, 3.2, 3.4_
  - _Boundary: buildSystemPrompt_

- [x] 2.3 (P) チャット送信処理が学年レベルをリクエストに含められるようにする
  - メッセージ送信・画像送信の処理が、呼び出し元から指定された学年レベルを送信内容に含められるようにする
  - _Requirements: 3.3_
  - _Boundary: useChat_

- [x] 2.4 チャットAPIが学年レベルを検証しAIコーチの応答に反映する
  - リクエストに含まれる学年レベルを検証し、未指定または不正な値の場合はエラーを返さず既定の学年レベルにフォールバックして処理を継続する
  - 検証済みの学年レベルをもとに組み立てた指示文をAIモデルへの指示として使用する
  - 統合テストで、学年レベルごとに異なる指示文がAIモデル呼び出しに使用されること・学年レベルが未指定または不正値でもエラーにならず既定値で処理が継続されることを確認する
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 4.1_
  - _Boundary: /api/chat route_
  - _Depends: 2.2_

- [x] 2.5 学年レベルの設定画面を実装する
  - 現在保存されている学年レベルを表示し、中学生・高校生を選択できる操作を提供する
  - 選択操作と保存操作を分離し、保存ボタンを押したときにのみ選択値を確定保存する
  - チャット画面へ戻る導線を設ける
  - 保存操作を行うと、保存済みの学年レベルが選択した値に更新されて画面に反映される
  - 統合テストで、画面表示時に現在の学年レベルが選択済み表示されること・選択を変更して保存すると値が更新されることを確認する
  - _Requirements: 2.2, 2.3_
  - _Boundary: SettingsPage_
  - _Depends: 2.1_

- [ ] 3. Integration — 画面導線とチャット画面への接続
- [x] 3.1 (P) チャット画面のメニューから学年レベル設定画面への導線を追加する
  - チャット画面のメニューに学年レベル設定画面へ遷移する項目を追加する
  - メニュー項目を選択すると学年レベル設定画面に遷移することを確認する
  - _Requirements: 2.1_
  - _Boundary: SessionDrawer_
  - _Depends: 2.5_

- [x] 3.2 (P) チャット画面が現在の学年レベルを取得し送信処理に反映する
  - チャット画面が保存済みの学年レベルを取得し、メッセージ・画像の送信処理に渡すようにする
  - 学年レベル設定画面で変更した後にチャット画面へ戻ると、変更後の学年レベルで送信されることを確認する
  - _Requirements: 3.3_
  - _Boundary: ChatPage_
  - _Depends: 2.1, 2.3_

- [ ] 4. Validation — エンドツーエンドの動作確認
- [x] 4.1 学年レベル設定からチャット応答までのゴールデンパスをエンドツーエンドで確認する
  - 設定画面から学年レベルを変更して保存し、チャット画面でメッセージを送信すると、変更後の学年レベルに応じた指示でAIコーチが応答することを確認する
  - 学年レベル未設定の初回利用時、中学生向けの説明でAIコーチが応答することを確認する
  - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3_
  - _Depends: 3.1, 3.2, 2.4_

- [x]* 4.2 学年レベルの保存に失敗してもチャット機能が継続することを検証する
  - 端末側のストレージが利用できない状況を模擬し、学年レベル設定操作後もチャット送信がブロックされず、既定またはそれまで有効だった学年レベルで動作が継続することを確認する
  - _Requirements: 4.1_
  - _Depends: 2.1_

- [x] 5. Foundation — 応答レベルの値表現・既定値・検証ロジックを実装する
  - 応答レベルとして「基本」「応用」の2値のみを許容する値の種類を定義する
  - 応答レベルが未設定の場合に用いる既定値（基本）を定義する
  - 任意の入力値が有効な応答レベルかどうかを判定できるようにする
  - ユニットテストで、有効な値（基本・応用）では真、無効な値（不正な文字列・undefined・null等）では偽と判定されることを確認する
  - _Requirements: 5.2, 5.4_
  - _Boundary: ResponseLevel型/バリデータ_

- [ ] 6. Core — 応答レベルの永続化とAIコーチングへの反映
- [x] 6.1 (P) 応答レベルを端末（ブラウザ）に保存・読み込みするロジックを実装する
  - マウント時に保存済みの応答レベルを同期的に読み込み、SSR環境でもエラーなく動作するようにする
  - 保存値が存在しない、または不正な値の場合は既定の応答レベル（基本）にフォールバックする
  - 応答レベルの変更操作を行うと、保存の成否に関わらずアプリ内の状態が即座に選択値へ更新される
  - 保存処理が例外をスローしても握りつぶし、アプリの動作を継続する
  - ユニットテストで、未設定時に既定値が返ること・保存済みの正常値を読み込めること・不正値保存時に既定値へフォールバックすること・保存処理が例外をスローしても状態更新自体は成功することを確認する
  - _Requirements: 2.4, 2.5, 4.2, 5.1, 5.2, 5.3, 5.4_
  - _Boundary: useResponseLevel_

- [x] 6.2 (P) AIコーチの指示文とチャットAPIを応答レベルに対応させる
  - 学年レベルに加えて応答レベルを受け取り、応答レベルに応じたヒントの発展度合いの指示を組み立てられるようにする
  - 基本レベルでは結論に近いわかりやすい一段階のヒント、応用レベルでは概念的な問いかけや別解の検討を促す発展的なヒントの指示を含める
  - 学年レベルの語彙指示と応答レベルのヒント発展度合い指示が独立して共存し、既存のヒント中心コーチング方針（答えを直接教えない等）はいずれの組み合わせでも共通して含まれるようにする
  - チャットAPIがリクエストに含まれる応答レベルを検証し、未指定または不正な値の場合はエラーを返さず既定の応答レベルにフォールバックして処理を継続する
  - 統合テストで、応答レベルごとに異なる指示文がAIモデル呼び出しに使用されること・応答レベルが未指定または不正値でもエラーにならず既定値で処理が継続されることを確認する
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 4.2_
  - _Boundary: buildSystemPrompt, /api/chat route_

- [x] 6.3 (P) チャット送信処理が応答レベルをリクエストに含められるようにする
  - メッセージ送信・画像送信の処理が、呼び出し元から指定された応答レベルを送信内容に含められるようにする
  - ユニットテストで、送信リクエストのボディに応答レベルが含まれることを確認する
  - _Requirements: 6.3_
  - _Boundary: useChat_

- [ ] 6.4 学年レベル・応答レベル設定画面に応答レベルの選択・保存UIを統合する
  - 現在保存されている応答レベルを、学年レベルと同一の設定画面上に表示し、基本・応用を選択できる操作を提供する
  - 既存の保存ボタンを押したときに、学年レベル・応答レベルの両方の選択値が確定保存される
  - 保存操作を行うと、保存済みの応答レベルが選択した値に更新されて画面に反映される
  - 統合テストで、画面表示時に現在の学年レベル・応答レベルが同一画面に選択済み表示されること・応答レベルの選択を変更して保存すると値が更新されることを確認する
  - _Requirements: 2.4, 2.5_
  - _Boundary: SettingsPage_
  - _Depends: 6.1_

- [ ] 7. Integration — 画面導線とチャット画面への接続更新
- [ ] 7.1 (P) チャット画面のメニューの設定画面への導線ラベルを応答レベル設定を含む名称に更新する
  - メニュー項目のラベルが学年レベル・応答レベルの両方を設定できる画面であることを示す文言に変わっていることを確認する
  - _Requirements: 2.1_
  - _Boundary: SessionDrawer_
  - _Depends: 6.4_

- [ ] 7.2 (P) チャット画面が現在の応答レベルを取得し送信処理に反映する
  - チャット画面が保存済みの応答レベルを取得し、メッセージ・画像の送信処理に渡すようにする
  - 学年レベル・応答レベル設定画面で変更した後にチャット画面へ戻ると、変更後の応答レベルで送信されることを確認する
  - _Requirements: 6.3_
  - _Boundary: ChatPage_
  - _Depends: 6.1, 6.3_

- [ ] 8. Validation — エンドツーエンドの動作確認
- [ ] 8.1 応答レベル設定からチャット応答までのゴールデンパスをエンドツーエンドで確認する
  - 設定画面から応答レベルを変更して保存し、チャット画面でメッセージを送信すると、変更後の応答レベルに応じたヒントでAIコーチが応答することを確認する
  - 応答レベル未設定の初回利用時、基本レベル向けのヒントでAIコーチが応答することを確認する
  - _Requirements: 2.1, 2.4, 2.5, 6.1, 6.2, 6.3_
  - _Depends: 7.1, 7.2, 6.2_

- [ ]* 8.2 応答レベルの保存に失敗してもチャット機能が継続することを検証する
  - 端末側のストレージが利用できない状況を模擬し、応答レベル設定操作後もチャット送信がブロックされず、既定またはそれまで有効だった応答レベルで動作が継続することを確認する
  - _Requirements: 4.2_
  - _Depends: 6.1_

## Implementation Notes

- Task 2.3: `UseChatOptions.gradeLevel` は design.md の型定義（必須）と異なり、意図的に任意（`gradeLevel?: GradeLevel`）として実装した。理由は `ChatPage`（タスク3.2の担当範囲）がまだ `gradeLevel` を渡していないため、必須にすると既存呼び出し箇所の型エラーになるため。タスク3.2で `useGradeLevel` の値を実際に渡すよう配線した時点で、実質的に常に値が渡される状態になる。タスク3.2実装時は、このオプショナル設計を前提にワイヤリングを忘れないよう注意すること。
- Task 4.1: `jest.setup.js` に `Headers`/`Request`/`Response` の簡易ポリフィルを追加し、jsdom環境で実際の `/api/chat` route.ts の `POST` ハンドラを直接呼び出せるようにした（`route.test.ts` と同様に `@/auth`・`@google/genai` のみモック）。レビューで、この簡易実装は大文字小文字を区別しない `Headers.get()` や `.append()` 未対応など仕様非準拠な点が指摘された（現状のテストには影響しないため非ブロッキング）。将来ヘッダーの大文字小文字やcookie等を扱うテストを追加する際は、依存関係に既に存在する `undici` の `Headers`/`Request`/`Response`（仕様準拠）へ置き換えることを検討すること。
- Task 3.1: `session-drawer.test.tsx` は `next-auth`/`@auth/core` の ESM import が原因で、以前はスイート全体がロード時にクラッシュしテストが1つも実行されていなかった（`jest.mock("@/components/auth/actions", ...)` で解消）。このモック追加により隠れていた既存バグ（Dialog.Popupのスタイルテストが `left-0` を期待していたが実装は `right-0`。コミット `a07592b` でドロワーが右寄せに変更された際にテストが未更新だった）が可視化されたため、`right-0` に修正して解消済み。他のタスクで `session-drawer.test.tsx` に触れる場合、このモックパターン（`@/components/auth/actions` のモック）を踏襲すること。
